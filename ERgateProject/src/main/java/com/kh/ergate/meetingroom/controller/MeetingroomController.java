package com.kh.ergate.meetingroom.controller;

import java.util.ArrayList;

import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

import com.kh.ergate.common.model.vo.PageInfo;
import com.kh.ergate.common.template.Pagination;
import com.kh.ergate.meetingroom.model.service.MeetingroomServiceImpl;

public class MeetingroomController {

	/*
	 * private MeetingroomServiceImpl meService;
	 * 
	 * @RequestMapping("currentStatus.me") public String
	 * selectCurrentStatusList(Model model) { int listCount =
	 * meService.selectCurrentStatusList();
	 * 
	 * PageInfo pi = Pagination.getPageInfo(listCount, currentPage, 10, 5);
	 * 
	 * ArrayList<MeetingroomReservation> list = meService.selectList(pi);
	 * 
	 * }
	 */
}
